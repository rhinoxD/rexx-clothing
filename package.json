{
  "name": "rexx-clothing-server",
  "version": "1.0.0",
  "engines": {
    "node": "14.17.1",
    "npm": "7.24.0"
  },
  "scripts": {
    "client": "cd client && npm start",
    "server": "nodemon server.js",
    "build": "cd client && npm run build",
    "dev": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\"",
    "start": "node server.js",
    "build2": "NPM_CONFIG_PRODUCTION=false && npm install && npm install --prefix client && npm run build --prefix client",
    "build3": "npm install && cd client && npm install --only=dev --no-shrinkwrap && npm run build",
    "heroku-postbuild": "cd client && npm install && npm install --only=dev --no-shrinkwrap && npm run build"
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "compression": "1.7.4",
    "cors": "2.8.5",
    "dotenv": "7.0.0",
    "express": "^4.16.4",
    "nodemailer": "^6.7.2",
    "react-icons": "^4.3.1",
    "stripe": "^6.28.0"
  },
  "devDependencies": {
    "concurrently": "^4.0.1",
    "nodemon": "^1.19.1"
  }
}
